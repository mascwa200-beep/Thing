<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Adaptive-icon foreground: an original LCARS swept-block glyph (accent panel + a secondary-hued
         inset accent bar) within the ~66dp safe zone — style homage only, not the trademarked Starfleet
         delta. Matches this app's own lcarsBlockShape/LcarsFrame silhouette (one swept corner, sharp
         elsewhere) so the launcher icon reads as a shrunken panel from the app itself. -->
    <path
        android:fillColor="#FFB000"
        android:pathData="M21,21 L61,21 A26,26 0 0 1 87,47 L87,87 L21,87 Z" />
    <path
        android:fillColor="#CC99CC"
        android:pathData="M21,68 L87,68 L87,78 L21,78 Z" />
</vector>
