<?xml version="1.0" encoding="utf-8"?>
<!-- Static preview for the widget picker. The live widget (widget_feed) uses an AdapterViewFlipper
     collection, which the launcher can't inflate as a static preview ("Can't load widget"); this
     plain layout stands in for the picker only. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="14dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PULSE · LIVE FEED"
        android:textColor="#5AD1FF"
        android:textStyle="bold"
        android:textSize="11sp"
        android:letterSpacing="0.12" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MARKETS"
            android:textColor="#5E708C"
            android:textStyle="bold"
            android:textSize="9sp"
            android:letterSpacing="0.15" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Live movers, fuel, economy &amp; headlines"
            android:textColor="#E6EFFA"
            android:textSize="14sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="NEWS"
            android:textColor="#5E708C"
            android:textStyle="bold"
            android:textSize="9sp"
            android:letterSpacing="0.15" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Auto-cycling on your home screen"
            android:textColor="#E6EFFA"
            android:textSize="14sp" />
    </LinearLayout>
</LinearLayout>
