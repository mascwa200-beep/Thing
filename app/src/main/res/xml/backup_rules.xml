<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Back up ONLY user settings/watchlists; including just datastore/ already excludes everything
         else (the regenerable network cache, the downloaded model, etc.). -->
    <include domain="file" path="datastore/" />
</full-backup-content>
