// ⚠️ Stubs DERIVED by copying the real declarations, never written from memory — a stub that does
// not match asserts the wrong thing is fine, which has cost this repo a CI round before.
package dev.mascwa.pulse.navigation
object Routes { const val SETTINGS = "settings" }
